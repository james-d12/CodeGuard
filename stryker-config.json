{
  "stryker-config": {
    "solution": "CodeGuard.sln",
    "mutate": ["**/*.cs", "!src/CodeGuard.Cli/**/*.cs", "!tests/CodeGuard.IntegrationTests/**/*.cs"],
    "reporters": ["progress", "html", "json"],
    "thresholds": {
      "high": 80,
      "low": 60,
      "break": 0
    }
  }
}
