<Project Sdk="Microsoft.NET.Sdk">

    <!-- Deliberate, minor deviation from the "no dependencies" invariant documented in CLAUDE.md:
         AnalysisModelBuilder needs ILogger<T>. Microsoft.Extensions.Logging.Abstractions is itself a
         zero-dependency leaf package, so this doesn't reintroduce any project-reference coupling. -->
    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
    </ItemGroup>

</Project>
